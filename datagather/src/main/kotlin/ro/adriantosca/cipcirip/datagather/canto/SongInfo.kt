package ro.adriantosca.cipcirip.datagather.canto

data class SongInfo(val birdCode: String,
                    val length: String,
                    val recordist: String,
                    val downloadLink: String,
                    val id: String
)
